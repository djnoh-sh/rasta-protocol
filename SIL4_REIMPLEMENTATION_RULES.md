# SIL4 Reimplementation Rules

## 1. Purpose

이 문서는 현재 `rasta-protocol` 저장소를 참고용 프로토타입으로 보고,
향후 SIL4 과제 대응용 재구현을 완료할 때까지 지켜야 할 작업 규칙을 정의한다.

이 문서의 목적은 다음 세 가지다.

1. 프로토타입 코드와 인증 대상 코드를 혼동하지 않도록 경계를 분명히 한다.
2. 코딩 스타일 수준을 넘어서 요구사항, 설계, 검증, 변경관리 규율을 강제한다.
3. 앞으로 수행하는 모든 설계/구현/리뷰/테스트 작업의 판단 기준을 통일한다.

## 2. Scope

- 본 규칙은 이 과제에서 새로 작성하는 요구사항, 설계 문서, 소스 코드,
  테스트 코드, 스크립트, 분석 결과, 검토 기록에 모두 적용한다.
- 기존 프로토타입 소스는 참고 자료로만 사용한다.
- 기존 프로토타입 소스의 동작을 그대로 복사하는 것은 재사용으로 간주하지 않는다.
  반드시 요구사항과 설계를 먼저 확정한 뒤 재구성해야 한다.

## 3. Rule Priority

우선순위는 다음과 같다.

1. 안전 및 인증 요구사항
2. 본 문서 `SIL4_REIMPLEMENTATION_RULES.md`
3. `CODING_RULES.md`
4. 기존 프로토타입 코드의 구현 방식

충돌 시 하위 규칙은 상위 규칙을 덮어쓸 수 없다.

## 4. Project Principles

- 본 과제의 목표는 "프로토타입 개선"이 아니라 "인증 가능한 재구현"이다.
- 구현보다 요구사항과 설계를 먼저 확정한다.
- 코드보다 추적성과 검증 가능성을 우선한다.
- 편의상 빠른 수정은 허용하지 않는다. 모든 수정은 요구사항, 설계, 테스트와 연결되어야 한다.
- "동작한다"는 것만으로 완료로 보지 않는다. 근거 문서와 검증 결과가 있어야 완료다.

## 5. Clean-Room Reimplementation Rules

- 기존 `src/rasta`, `src/sci`, `examples` 코드는 동작 참고 자료로만 사용한다.
- 새 구현은 별도 디렉터리, 별도 네이밍, 별도 문서 체계로 관리한다.
- 기존 함수명, 구조체명, 파일 구조를 그대로 답습하지 않는다.
- 프로토타입 코드를 직접 복사해 시작하지 않는다.
- 프로토타입에서 가져올 수 있는 것은 다음으로 제한한다.
  - 프로토콜 개념
  - 패킷 필드 정의
  - 상태 전이 아이디어
  - 테스트 시나리오 후보
  - 위험 포인트 목록
- 프로토타입에서 가져오면 안 되는 것은 다음과 같다.
  - 코드 블록 직접 복사
  - 검증되지 않은 상수/타이밍 값 무비판적 재사용
  - 주석과 문서의 무검증 재사용

## 6. Work Item Rules

모든 작업은 아래 단위를 기준으로 수행한다.

- 하나의 작업은 하나의 목적만 가진다.
- 하나의 작업은 반드시 `요구사항 ID`, `설계 항목 ID`, `테스트 ID` 중 최소 하나와 연결되어야 한다.
- 요구사항 ID 없이 구현만 먼저 시작하지 않는다.
- 리뷰 없이 "완료"로 표시하지 않는다.
- 작업 종료 시 다음 네 가지를 남긴다.
  - 무엇을 바꿨는가
  - 왜 바꿨는가
  - 어떤 요구사항과 연결되는가
  - 어떻게 검증했는가

## 7. Requirements Rules

- 요구사항은 기능 요구사항과 안전 요구사항을 분리해 관리한다.
- 모든 요구사항은 고유 ID를 가진다.
- 각 요구사항은 검증 방법을 반드시 가진다.
- "빠르다", "안전하다", "적절하다" 같은 모호한 표현을 금지한다.
- 시간 제약, 시퀀스 제약, 오류 처리, fail-safe 동작은 수치와 조건으로 명시한다.
- 요구사항이 확정되지 않은 기능은 구현하지 않는다.
- 요구사항 변경 시 관련 설계, 코드, 테스트, 문서를 함께 갱신한다.

## 8. Architecture and Design Rules

- 설계는 최소한 `상위 설계(HLD)`와 `상세 설계(LLD)`로 분리한다.
- 설계 문서 없이 새 모듈을 만들지 않는다.
- 모듈은 단일 책임 원칙을 우선 적용한다.
- 파싱, 상태 머신, 타이머, 전송, 진단, 로깅, 설정 관리는 분리한다.
- 안전 관련 로직과 편의 기능 로직을 같은 모듈에 섞지 않는다.
- 전역 상태는 최소화한다.
- 암묵적 상태 전이보다 명시적 상태 전이를 사용한다.
- 상태 머신은 반드시 도식과 표로 관리한다.

## 9. Coding Rules

- `CODING_RULES.md`의 규칙과 MISRA-C:2012 핵심 항목을 따른다.
- 동적 메모리 사용은 원칙적으로 금지한다.
- 불가피한 경우에도 승인된 메모리 정책과 수명 규칙이 문서화되어야 한다.
- 재귀 호출은 금지한다.
- `goto` 사용은 금지한다.
- 모든 변수는 선언 즉시 초기화한다.
- 모든 `switch`는 `default`와 명시적 종료를 가져야 한다.
- 매직 넘버는 금지한다. 명명된 상수 또는 설정값으로 치환한다.
- 함수는 짧고 단일 목적이어야 한다.
- 부수효과가 큰 함수는 이름에서 그 의도를 드러내야 한다.
- 오류 코드는 무시하지 않는다.
- 무시가 필요한 경우 그 이유를 주석으로 남긴다.

## 10. Naming and Structure Rules

- 새로운 인증 대상 코드는 기존 프로토타입 네이밍을 그대로 따르지 않는다.
- 함수명, 타입명, 파일명은 모듈 책임이 드러나야 한다.
- API 계층과 내부 구현 계층을 분리한다.
- 헤더는 public/internal로 구분한다.
- 테스트 디렉터리 구조는 제품 코드 구조를 따라간다.

## 11. Interface Rules

- 외부 인터페이스는 명시적 계약을 가진다.
- 입력 조건, 출력 조건, 오류 조건, 타이밍 제약을 문서화한다.
- 바이트 오더, 패딩, 길이 필드, 체크섬 범위는 인터페이스 명세에 포함한다.
- 플랫폼 의존 코드는 추상화 계층 아래로 격리한다.

## 12. State Machine Rules

- 상태 머신은 표와 PlantUML 둘 다 유지한다.
- 허용되지 않은 이벤트 수신 시 동작을 명시한다.
- timeout, retransmission, disconnect, initialization, shutdown은 모두 상태 전이에 포함한다.
- 상태 전이는 테스트 케이스와 1:1 또는 1:N 관계로 추적 가능해야 한다.
- 상태 값은 외부에서 임의 변경할 수 없도록 캡슐화한다.

## 13. Timing and Determinism Rules

- 시간 관련 값은 문서화된 요구사항 또는 설정에 근거해야 한다.
- monotonic clock만 사용한다.
- blocking call 사용 여부는 설계 문서에 명시한다.
- 대기, 재시도, timeout은 모두 상한 시간을 가져야 한다.
- 비결정적 동작은 허용하지 않는다. 허용이 불가피하면 원인과 범위를 문서화한다.

## 14. Error Handling Rules

- 모든 오류는 감지, 전파, 기록, 안전한 처리 경로를 가져야 한다.
- 오류 로그만 남기고 계속 진행하는 방식은 금지한다.
- "should never happen"으로 끝나는 코드는 금지한다.
- 복구 가능한 오류와 복구 불가능한 오류를 구분한다.
- 복구 불가능한 오류는 fail-safe 동작으로 연결되어야 한다.

## 15. Configuration Rules

- 설정값은 타입, 범위, 기본값, 허용 단위가 정의되어야 한다.
- 런타임 설정과 빌드타임 설정을 구분한다.
- 안전 동작에 영향을 주는 설정은 부팅 시 검증한다.
- 잘못된 설정은 조용히 보정하지 않는다.
- 설정 파서는 명세 기반 테스트를 가져야 한다.

## 16. Logging and Diagnostics Rules

- 로그는 디버깅용 로그와 안전 진단용 로그를 구분한다.
- 안전 판단에 필요한 정보는 구조적으로 기록한다.
- 로그 포맷은 파싱 가능해야 한다.
- 진단 정보는 시간, 상태, 이벤트, 원인, 영향 범위를 포함해야 한다.
- 로그가 시스템 동작을 바꾸지 않도록 한다.

## 17. Test Rules

- 모든 요구사항은 최소 하나의 검증 항목을 가진다.
- 단위 테스트, 통합 테스트, 경계값 테스트, 오류 주입 테스트를 구분한다.
- 상태 머신은 상태/이벤트 조합 기준으로 테스트한다.
- 체크섬, 직렬화, 역직렬화는 known-answer test를 포함한다.
- 프로토타입에서 확인된 결함은 회귀 테스트로 고정한다.
- 테스트 이름만 보고 무엇을 검증하는지 알 수 있어야 한다.
- 테스트가 없으면 기능 완료로 인정하지 않는다.
- 검증 실행 순서는 `configure -> build -> build 종료 확인 -> test -> static analysis`를 기본 규칙으로 한다.
- `build`가 끝나기 전에 test binary를 실행하지 않는다.
- `build`와 `test`를 병렬 실행하지 않는다.
- `cppcheck` 같은 정적 분석도 build와 병렬 실행하지 않는다.
- 빠른 확인이 필요해도 검증 단계 간 순서를 깨는 임시 병렬 실행은 허용하지 않는다.
- 반복 패턴 확장 작업은 큰 블록 자동 치환보다 `함수 1개 + registration 1개 + 문서 1세트` 단위의 좁은 수정으로 나눈다.
- 반복 패턴 확장에서 자동 복제나 치환을 썼다면 build 전에 새로 추가된 함수 블록만 별도로 다시 읽어 progression, registration, 정의 순서를 검산한다.
- holdoff/remaining/count 같은 수열형 값은 반복 확장 직후 해당 함수 범위 안에서 직접 검산하고, 넓은 범위 일괄 치환만으로 완료 처리하지 않는다.
- 자동 치환이 기존 wrapper, registration, 인접 함수까지 함께 건드릴 가능성이 있으면 더 작은 범위의 수동 수정으로 되돌린다.

## 18. Static Analysis and Quality Gate Rules

- 컴파일 경고 0건을 기본 원칙으로 한다.
- 새 코드에는 `-Werror` 수준의 품질 게이트를 적용한다.
- 정적 분석 결과는 작업 종료 전에 확인한다.
- MISRA 편차가 발생하면 편차 사유와 승인 기록을 남긴다.
- 분석 경고를 "나중에 처리"로 남기지 않는다. 예외는 별도 관리표에 등록한다.
- `cppcheck-suppress` 주석은 필요한 최소 개수만 사용한다.
- 같은 suppress 주석을 추가할 때는 바로 위/아래 인접 줄에 동일 suppress가 이미 있는지 먼저 확인한다.
- 인접한 동일 suppress 주석이 이미 있으면 중복 추가하지 않는다.

## 19. Review Rules

- 구현자는 자기 코드를 스스로 승인할 수 없다.
- 모든 변경은 코드 리뷰와 설계 리뷰 중 최소 하나를 거친다.
- 안전 관련 변경은 독립 검토를 우선한다.
- 리뷰 결과는 "문제 없음"이 아니라 "무엇을 확인했고 어떤 리스크를 보았는지"가 남아야 한다.

## 20. Traceability Rules

- 요구사항 -> 설계 -> 코드 -> 테스트의 추적 링크를 유지한다.
- 각 커밋 또는 작업 단위는 관련 ID를 포함한다.
- 문서와 코드가 어긋나면 문서를 먼저 갱신하거나 작업을 중단한다.
- 추적되지 않는 코드는 임시 코드로 간주하며 병합하지 않는다.

## 21. Change Management Rules

- 변경은 작은 단위로 나눈다.
- 하나의 변경에 너무 많은 책임을 섞지 않는다.
- 인터페이스 변경은 영향도 분석을 먼저 수행한다.
- 안전 관련 상수 변경은 테스트와 문서를 동시에 갱신한다.
- 미완성 기능은 feature flag 없이 mainline에 넣지 않는다.
- `git add`, `git commit`, `git rm`, `git mv`, `git restore --staged`처럼 index/worktree를 쓰는 git 명령은 순차 실행만 허용한다.
- index를 쓰는 git 명령을 병렬 실행하지 않는다.
- staging과 commit은 기본적으로 `git add -> staged diff 확인 -> git commit` 순서로만 진행한다.
- `index.lock` 오류가 나면 먼저 동시 실행 여부와 살아있는 git 프로세스를 확인하고, stale lock 여부가 확인되기 전에는 임의 삭제하지 않는다.

## 22. Documentation Rules

- 구현 전에 설계 문서를 먼저 만든다.
- 구현 후에는 설계 문서와 차이를 즉시 반영한다.
- 시퀀스 다이어그램, 상태도, 인터페이스 표는 코드와 같이 유지한다.
- 문서는 "설명"이 아니라 "판단 기준"이 되도록 작성한다.
- 문서에 예외 사항이 있으면 이유와 범위를 함께 적는다.

## 23. Definition of Done

다음 조건을 모두 만족해야 작업이 완료다.

- 요구사항 ID가 있다.
- 설계 반영이 끝났다.
- 코드가 규칙을 만족한다.
- 테스트가 추가되었고 통과했다.
- 정적 분석에서 새로운 경고가 없다.
- 리뷰가 끝났다.
- 추적 링크가 갱신되었다.
- 관련 문서가 갱신되었다.

하나라도 빠지면 완료가 아니다.

## 24. Explicit Prohibitions

다음은 명시적으로 금지한다.

- 프로토타입 코드 직접 복사
- 경고가 있는 상태로 완료 처리
- 테스트 없이 기능 병합
- 요구사항 없이 코드부터 작성
- 문서 없이 구조 변경
- 동작 확인만 하고 안전성 검토 생략
- 임시 우회 코드의 장기 방치
- 설계와 구현 불일치를 알고도 넘어가기

## 25. Practical Working Agreement for This Repository

앞으로 이 저장소에서 이 과제를 진행할 때는 다음을 기본 동작으로 본다.

- 새 구현 관련 작업을 시작할 때 먼저 본 문서를 확인한다.
- 코드를 수정하거나 추가하기 전 관련 요구사항/설계 항목을 먼저 정리한다.
- 작업 종료 시 `CODING_RULES.md`와 본 문서 기준으로 자체 점검한다.
- 가능하면 문서, 테스트, 코드 순서로 작업하고, 최소한 코드와 테스트는 같은 작업 단위에서 함께 갱신한다.
- 안전 관련 판단이 애매하면 구현을 미루고 규칙/설계/요구사항을 먼저 명확히 한다.
- 로컬 검증과 CI 검증 모두 `build 완료 후 test 실행` 원칙을 따른다.
- 검증 로그를 분리해 남기고, test 실패 해석 전에 해당 시점 build가 실제로 끝났는지 먼저 확인한다.
