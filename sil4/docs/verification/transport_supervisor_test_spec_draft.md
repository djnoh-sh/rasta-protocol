# Test Specification Draft - Transport Supervisor

## Document Control

- Document ID: `TS-009`
- Version: `0.1.0`
- Status: `Draft`
- Owner: `Project Team`
- Last Updated: `2026-03-17`
 - Last Updated: `2026-03-20`

## Scope

- 테스트 대상:
  - `MOD-004 Transport Supervisor`
- 관련 요구사항:
  - `FR-003`
  - `FR-004`
  - `SR-002`
- 테스트 레벨: `Unit`

## Test Cases

| Test ID | Req ID | Objective | Precondition | Stimulus | Expected Result | Pass/Fail Criteria |
| --- | --- | --- | --- | --- | --- | --- |
| TC-SUP-001 | FR-003, FR-004 | inbound frame handoff 검증 | `CONNECTING` 상태 session, codec decode stub 준비 | `FRAME_RECEIVED` frame 처리 | decoded event가 session에 전달되어 `ESTABLISHED` 전이 | decoded message, effective event, session status, decision이 설계와 일치 |
| TC-SUP-002 | SR-002 | invalid argument 방어 검증 | null context 또는 invalid frame | supervisor init/process 호출 | `INVALID_ARGUMENT` 반환 | UB 없이 결정적 오류 처리 |
| TC-SUP-003 | SR-002 | decode failure 방어 검증 | `CONNECTING` 상태 session, codec이 `DECODE_ERROR` 반환하도록 준비 | `FRAME_RECEIVED` frame 처리 | `DECODE_FAILED` 반환, session 미호출, processed count 미증가 | session state가 유지되고 supervisor report가 failure decision을 보존 |
| TC-SUP-004 | FR-003, SR-002 | unsupported message 방어 검증 | `CONNECTING` 상태 session, codec이 `UNSUPPORTED_MESSAGE` 반환하도록 준비 | 지원하지 않는 message frame 처리 | `DECODE_FAILED` 반환, decoded type은 기록되지만 session 미호출 | unsupported message가 보고서에 남고 상태 전이는 발생하지 않음 |
| TC-SUP-005 | FR-004 | sequence gap 검증 | `ESTABLISHED` 상태 session, 먼저 in-order frame 기록 | sequence가 건너뛴 frame 처리 | `RETRANSMISSION_PENDING` 전이 | supervisor가 gap을 감지해 retransmission 경로를 연다 |
| TC-SUP-006 | SR-001 | stale sequence protocol error 검증 | `ESTABLISHED` 상태 session, 먼저 in-order frame 기록 | duplicate/stale frame 처리 | `SAFE_DISCONNECT` 전이 | supervisor가 stale sequence를 protocol error로 처리한다 |
| TC-SUP-007 | FR-003 | poll receive handshake 경로 검증 | available channel, receive가 handshake frame 반환하도록 준비 | `poll_receive` 호출 | `ESTABLISHED` 전이, poll/receive count 증가 | runtime loop entry가 direct frame path와 동일 동작을 보장 |
| TC-SUP-008 | SR-002 | poll receive channel down/no-frame 검증 | unavailable channel 또는 receive unavailable 준비 | `poll_receive` 호출 | `CHANNEL_DOWN` 또는 `NO_FRAME` 반환 | channel gate가 decode/session 호출보다 우선 적용되고 decision이 보고된다 |
| TC-SUP-020 | SR-002 | poll receive transient error budget 검증 | available channel, generic receive error 준비 | `poll_receive` 호출 | 첫 receive error는 ignored event로 budget 처리 | transient receive error가 즉시 fail-safe로 확대되지 않고 decision/telemetry가 남는다 |
| TC-SUP-021 | SR-002 | poll receive repeated error escalation 검증 | `ESTABLISHED` 상태 session, generic receive error 반복 준비 | `poll_receive`를 연속 호출 | budget 도달 시 `PROTOCOL_ERROR`로 fail-safe 전이 | receive error budget reset, effective event, session status, error class telemetry가 설계와 일치 |
| TC-SUP-013 | FR-003, SR-002 | bounded pump receive drain 검증 | `ESTABLISHED` 상태 session, scripted receive/decode 준비 | `pump_receive(max_polls)` 호출 | 여러 frame 처리 후 `NO_FRAME`에서 정상 종료 | pump iteration/frame count와 terminal decision이 설계와 일치 |
| TC-SUP-014 | SR-002 | bounded pump invalid argument 검증 | null context 또는 zero max poll | `pump_receive` 호출 | `INVALID_ARGUMENT` 반환 | bounded runtime loop entry가 결정적으로 방어된다 |
| TC-SUP-009 | SR-002 | transport send failure budget 검증 | `ESTABLISHED` 상태 session 준비 | `SEND_FAILED` transport event를 연속 처리 | 첫 실패는 ignored, budget 도달 시 `SAFE_DISCONNECT` 전이 | transient failure와 persistent failure를 구분하고 decision/effective event, budget update/reset telemetry를 보고 |
| TC-SUP-010 | FR-003 | transport send completed ignored 검증 | `INITIALIZED` 상태 session 준비, outstanding send 없음 | `SEND_COMPLETED` transport event 처리 | `IGNORED_EVENT` 반환, 상태 유지 | correlation 없는 transport feedback은 stale event로 무시되고 decision이 남는다 |
| TC-SUP-018 | FR-003 | transport send completed correlation 검증 | `CONNECTING` 상태 session 준비, connect request 송신으로 outstanding send 존재 | matching `SEND_COMPLETED` transport event 처리 | `IGNORED_EVENT` 반환, outstanding send clear | correlation이 맞는 완료 feedback만 정상 소거로 처리되고 이후 stale completion과 구분된다 |
| TC-SUP-019 | FR-003 | deferred queue telemetry 검증 | outstanding send 1건과 deferred send 1건이 존재하는 session 준비 | matching `SEND_COMPLETED` transport event 처리 | deferred send가 즉시 dispatch되고 supervisor report가 refreshed queue 상태와 peak deferred depth를 함께 보고 | `uOutstandingSendPresent=1`, `uDeferredSendPresent=0`, `uDeferredSendCount=0`, `uQueuedSendCount=1`, `uMaxDeferredSendCount=1`, `uDeferredDispatchCount=1`, `uQueueOverflowRejectCount=0` |
| TC-SUP-033 | FR-003, IF-001 | supervisor report busy reject telemetry 검증 | `uBusyRejectErrorThreshold=2` configured established session과 current bounded queue saturation 상태 준비 | fourth deferred fill 뒤 reject 2회를 만든 뒤 matching `SEND_COMPLETED`로 report refresh를 수행 | supervisor report가 overflow/busy reject count, current streak reset, max streak, escalation count, latch reset을 current adapter telemetry와 일치하게 반영한다 | `uQueueOverflowRejectCount`, `uBusyRejectedSendCount`, `uConsecutiveBusyRejectedSendCount`, `uMaxConsecutiveBusyRejectedSendCount`, `uBusyRejectEscalationCount`, `uLastBusyRejectEscalated`가 설계와 일치 |
| TC-SUP-059 | FR-003, IF-001 | queue report matrix 검증 | correlated `SEND_COMPLETED` deferred redispatch path와 busy reject telemetry refresh path를 모두 재현 가능한 established session-supervisor 준비 | `transport send completed correlation`, `supervisor report busy reject telemetry`를 representative wrapper로 실행 | current queue report 규칙군에서 deferred redispatch 이후 refreshed queue state와 busy reject refresh telemetry가 representative wrapper 수준에서도 서로 모순 없이 유지된다 | representative wrapper가 current queue report/unit parity를 별도 closeout 항목으로 추적 가능하게 유지한다 |
| TC-SUP-015 | FR-003, SR-002 | channel down failover 검증 | `ESTABLISHED` 상태 session, active-standby channel manager, primary down/secondary up 상태 준비 | `CHANNEL_DOWN(primary)` transport event 처리 후 inactive primary의 `SEND_FAILED` 처리 | `IGNORED_EVENT` 반환, active channel이 secondary로 전환되고 stale primary failure는 budget에 반영되지 않음 | session state 유지, selected channel, failover decision, stale send failure ignore policy, send failure budget reset telemetry, supervisor channel switch telemetry가 설계와 일치 |
| TC-SUP-017 | FR-003, SR-002 | channel up refresh 검증 | `ESTABLISHED` 상태 session, failover로 secondary active 상태, primary restored 상태 준비 | `CHANNEL_UP(primary)` transport event 처리 후 stale secondary `SEND_FAILED`, recovered primary `SEND_FAILED` 처리 | `IGNORED_EVENT` 반환, active selection이 refresh되어 primary 복귀하고 recovered primary failure는 fresh budget으로 처리됨 | session state 유지, selected channel, decision telemetry, switch telemetry, budget channel telemetry가 설계와 일치 |
| TC-SUP-012 | SR-002 | send failure budget reset 검증 | `ESTABLISHED` 상태 session 준비 | send failure 후 정상 inbound frame 처리, 다시 send failure 처리 | budget이 reset되어 다시 ignored 처리 | 정상 traffic이 누적 send failure를 해소하고 budget reset telemetry가 설계와 일치 |
| TC-SUP-022 | FR-003, SR-002 | budget scope matrix 검증 | `ESTABLISHED` 상태 active-standby session, secondary failover/primary recovery 가능 상태 준비 | secondary correlated `SEND_FAILED`, primary recovery, stale secondary `SEND_FAILED`, primary correlated `SEND_FAILED`, primary `RX_ERROR`, secondary failover, secondary `RX_ERROR`를 순차 처리 | send failure budget은 channel-scoped로 stale inactive-channel feedback에 영향받지 않고 channel switch 시 reset-and-increment 되며, receive error budget은 channel switch를 넘어 carry-over되어 다음 오류에서 escalation된다 | budget channel/update telemetry, send/receive counter reset, receive escalation decision, final fail-safe state가 설계와 일치 |
| TC-SUP-023 | FR-003, SR-002 | send feedback ordering matrix 검증 | `ESTABLISHED` 상태 active-standby session, secondary failover/primary recovery 가능 상태와 outstanding send correlation 준비 | `secondary send -> primary recovery -> inactive secondary SEND_FAILED -> inactive secondary SEND_COMPLETED -> primary send -> primary SEND_FAILED`를 순차 처리 | inactive secondary `SEND_FAILED`는 outstanding가 있어도 active channel이 아니므로 ignored되고 outstanding/budget을 유지하며, inactive secondary `SEND_COMPLETED`는 outstanding correlation만으로 clear를 수행하고, 이후 primary correlated `SEND_FAILED`만 primary budget `1`로 반영된다 | decision kind, outstanding clear semantics, budget channel/update telemetry, final established state가 설계와 일치 |
| TC-SUP-024 | FR-003, SR-002 | channel event ordering matrix 검증 | `ESTABLISHED` 상태 active-standby session, failover와 recovery refresh가 가능한 channel 상태 준비 | `CHANNEL_DOWN(primary) -> CHANNEL_UP(secondary no-op) -> CHANNEL_UP(primary recovery) -> CHANNEL_UP(primary repeated no-op)`를 순차 처리 | failover와 preferred primary recovery refresh는 모두 actual selection switch로 기록되고, no-op refresh는 decision은 남기되 switch count를 증가시키지 않으며, switch가 발생한 경우 report는 `switch kind`와 `from/to channel`을 함께 남긴다 | selected channel, decision type, `uChannelSwitchCount`, `uLastChannelSwitchOccurred`, `eLastSwitchKind`, `eLastSwitchFromChannelId`, `eLastSwitchToChannelId`, final established state가 설계와 일치 |
| TC-SUP-034 | FR-003, SR-002 | switch audit closeout matrix 검증 | switch kind/from/to와 no-op refresh parity를 재현 가능한 active-standby session-supervisor 준비 | `channel event ordering matrix`를 closeout wrapper로 실행 | switch audit 규칙군에서 failover, preferred recovery, no-op refresh taxonomy가 서로 모순 없이 유지된다 | representative wrapper가 switch audit 규칙군의 unit coverage를 하나의 closeout 항목으로 추적 가능하게 유지한다 |
| TC-SUP-035 | FR-003, SR-002 | switch audit cumulative matrix 검증 | failover/preferred recovery/no-op refresh cumulative count parity를 재현 가능한 active-standby session-supervisor 준비 | `channel event ordering matrix`를 cumulative wrapper로 실행 | switch audit 규칙군에서 failover/preferred recovery cumulative count는 actual switch에서만 증가하고 no-op refresh count는 no-op refresh에서만 증가하며 preferred-triggered vs non-preferred-triggered no-op origin count와 refresh-total origin count도 함께 유지된다 | `uFailoverSwitchCount`, `uPreferredRecoverySwitchCount`, `uNoOpRefreshCount`, `uPreferredChannelTriggeredRefreshEventCount`, `uNonPreferredChannelTriggeredRefreshEventCount`, `uPreferredChannelTriggeredNoOpRefreshCount`, `uNonPreferredChannelTriggeredNoOpRefreshCount`, `eLastSwitchKind`, `uLastChannelSwitchOccurred`가 설계와 일치 |
| TC-SUP-036 | FR-003, SR-002 | switch audit envelope matrix 검증 | switch audit의 taxonomy/cumulative/reason/holdoff-reset representative path를 모두 재현 가능한 active-standby session-supervisor 준비 | `switch audit closeout matrix`, `switch audit cumulative matrix`, `switch audit reason matrix`, `switch audit holdoff outcome matrix`를 envelope wrapper로 실행 | switch audit 규칙군에서 last-event taxonomy, cumulative count parity, immediate-vs-after-holdoff preferred recovery reason parity, immediate-vs-after-holdoff preferred recovery cumulative count parity, holdoff-vs-active no-op reason parity, preferred-vs-non-preferred no-op origin parity, preferred-vs-non-preferred actual-switch trigger origin parity, holdoff outcome subtype parity, last holdoff cycle state parity가 representative wrapper 수준에서도 서로 모순 없이 유지된다 | representative wrapper가 current switch audit policy coverage를 하나의 unit closeout 항목으로 추적 가능하게 유지한다 |
| TC-SUP-037 | FR-003, SR-002 | switch audit reason matrix 검증 | holdoff no-op, immediate preferred recovery switch, ordinary holdoff-complete preferred recovery switch, bypass preferred recovery switch, already-active repeated refresh no-op을 모두 재현 가능한 active-standby session-supervisor 준비 | `channel event ordering matrix`, `switch audit holdoff progress matrix`, `switch audit trigger origin matrix`를 reason wrapper로 실행 | switch audit 규칙군에서 failover reason, immediate preferred recovery reason, ordinary holdoff-complete preferred recovery reason, bypass preferred recovery reason, holdoff no-op reason, active refresh no-op reason이 서로 구분되어 누적 count와 함께 유지되고 각 audit record는 trigger channel/event까지 남기며 ordinary completed holdoff cycle과 bypass-completed holdoff cycle도 분리 관찰되고 last completed holdoff cycle subtype도 유지된다 | `eLastSwitchReason`, `uImmediatePreferredRecoverySwitchCount`, `uHoldoffPreferredRecoverySwitchCount`, `uCompletedHoldoffPreferredRecoverySwitchCount`, `uBypassPreferredRecoverySwitchCount`, `uCompletedHoldoffCycleCount`, `uOrdinaryCompletedHoldoffCycleCount`, `uBypassCompletedHoldoffCycleCount`, `eLastCompletedHoldoffCycleKind`, `uHoldoffRefreshNoOpCount`, `uActiveRefreshNoOpCount`, `uPreferredChannelTriggeredNoOpRefreshCount`, `uNonPreferredChannelTriggeredNoOpRefreshCount`, `uPreferredChannelTriggeredSwitchCount`, `uNonPreferredChannelTriggeredSwitchCount`, `eLastSwitchTriggerEventType`, `eLastSwitchTriggerChannelId`가 설계와 일치 |
| TC-SUP-038 | FR-003, SR-002 | switch audit holdoff progress matrix 검증 | holdoff no-op, recovery reset, already-active repeated refresh를 모두 재현 가능한 active-standby session-supervisor 준비 | `channel event ordering matrix`를 holdoff-progress wrapper로 실행 | switch audit 규칙군에서 holdoff no-op 시 current stable-selection progress가 증가하고 preferred recovery switch 후 reset되며 already-active repeated refresh에서는 progress가 다시 누적되지 않는다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffTargetCount`, `uPreferredRecoveryHoldoffRemainingCount`, `eLastSwitchReason`가 설계와 일치 |
| TC-SUP-039 | FR-003, SR-002 | switch audit holdoff reset matrix 검증 | holdoff no-op 뒤 preferred primary `CHANNEL_DOWN`으로 reset과 renewed hold를 재현 가능한 active-standby session-supervisor 준비 | `CHANNEL_DOWN(primary failover) -> CHANNEL_UP(primary hold) -> CHANNEL_DOWN(primary reset) -> CHANNEL_UP(primary renewed hold)`를 순차 처리 | switch audit 규칙군에서 preferred primary flap down은 current stable-selection progress를 reset하고 no-switch audit reason으로 별도 기록되며 이후 hold는 다시 첫 progress부터 누적된다 | `uHoldoffResetCount`, `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, `eLastSwitchReason`, `eLastSwitchTriggerEventType`, `eLastSwitchTriggerChannelId`가 설계와 일치 |
| TC-SUP-040 | FR-003, SR-002 | switch audit holdoff cycle matrix 검증 | holdoff no-op cycle과 reset 이후 renewed hold cycle을 재현 가능한 active-standby session-supervisor 준비 | `switch audit holdoff progress matrix`, `switch audit holdoff reset matrix`를 cycle wrapper로 실행 | switch audit 규칙군에서 holdoff no-op의 첫 진입만 cycle로 누적되고 preferred recovery는 completed cycle로 누적되며 ordinary completed cycle과 bypass-completed cycle은 별도 bucket으로 유지되고 last completed holdoff cycle subtype도 ordinary/bypass로 유지되며 active refresh는 cycle/completed count를 증가시키지 않으며 reset 뒤 renewed hold는 새 cycle로 다시 기록된다 | `uHoldoffCycleCount`, `uCompletedHoldoffCycleCount`, `uOrdinaryCompletedHoldoffCycleCount`, `uBypassCompletedHoldoffCycleCount`, `eLastCompletedHoldoffCycleKind`, `uHoldoffRefreshNoOpCount`, `uHoldoffResetCount`, `uPreferredRecoveryHoldoffProgressCount`가 설계와 일치 |
| TC-SUP-041 | FR-003, SR-002 | switch audit trigger origin matrix 검증 | failover, holdoff no-op, active-loss bypass를 모두 재현 가능한 active-standby session-supervisor 준비 | `CHANNEL_DOWN(primary failover) -> CHANNEL_UP(primary hold) -> CHANNEL_DOWN(secondary bypass)`를 순차 처리 | switch audit 규칙군에서 preferred channel이 trigger인 actual switch와 non-preferred channel이 trigger인 actual switch가 누적 기준으로 구분되고 hold no-op은 actual-switch origin count를 오염시키지 않는다 | `uPreferredChannelTriggeredSwitchCount`, `uNonPreferredChannelTriggeredSwitchCount`, `eLastSwitchTriggerChannelId`, `eLastSwitchReason`가 설계와 일치 |
| TC-SUP-042 | FR-003, SR-002 | switch audit holdoff outcome matrix 검증 | ordinary holdoff-complete path, holdoff reset path, bypass holdoff-complete path를 모두 재현 가능한 active-standby session-supervisor 준비 | `switch audit holdoff progress matrix`, `switch audit holdoff reset matrix`, `switch audit trigger origin matrix`를 holdoff-outcome wrapper로 실행 | switch audit 규칙군에서 holdoff outcome은 ordinary complete, bypass complete, abort path로 분리되어 cumulative count, direct holdoff cycle start trigger, last completed subtype, last terminal outcome taxonomy, last terminal outcome trigger, terminal outcome subtype cumulative count, terminal outcome total cumulative count, terminal outcome trigger-origin cumulative count, terminal outcome trigger-event cumulative count까지 wrapper 기준에서 함께 유지된다 | `uCompletedHoldoffCycleCount`, `uOrdinaryCompletedHoldoffCycleCount`, `uBypassCompletedHoldoffCycleCount`, `uAbortedHoldoffCycleCount`, `uTerminalHoldoffOutcomeCount`, `uOrdinaryTerminalHoldoffOutcomeCount`, `uBypassTerminalHoldoffOutcomeCount`, `uAbortedTerminalHoldoffOutcomeCount`, `uPreferredChannelTriggeredTerminalHoldoffOutcomeCount`, `uNonPreferredChannelTriggeredTerminalHoldoffOutcomeCount`, `uChannelUpTriggeredTerminalHoldoffOutcomeCount`, `uChannelDownTriggeredTerminalHoldoffOutcomeCount`, `eLastHoldoffCycleStartTriggerEventType`, `eLastHoldoffCycleStartTriggerChannelId`, `eLastCompletedHoldoffCycleKind`, `eLastTerminalHoldoffOutcome`, `eLastTerminalHoldoffOutcomeTriggerEventType`, `eLastTerminalHoldoffOutcomeTriggerChannelId`, `eLastHoldoffCycleState`가 설계와 일치 |
| TC-SUP-043 | FR-003, SR-002 | switch audit terminal outcome envelope matrix 검증 | current terminal outcome family representative path를 모두 재현 가능한 active-standby session-supervisor 준비 | `switch audit holdoff outcome matrix`, `switch audit terminal outcome mixed long-run matrix`를 terminal-outcome envelope wrapper로 실행 | current switch audit 규칙군에서 terminal outcome family는 taxonomy, last trigger, subtype count, total count, trigger-origin count, trigger-event count를 representative wrapper 수준에서도 서로 모순 없이 유지하고 ordinary complete, abort, bypass complete가 한 run 안에서 누적돼도 cumulative parity를 유지한다 | representative wrapper가 current terminal outcome policy coverage를 하나의 unit closeout 항목으로 추적 가능하게 유지한다 |
| TC-SUP-044 | FR-003, SR-002 | switch audit terminal outcome threshold-three mixed long-run matrix 검증 | active-standby holdoff `3` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `3`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-045 | FR-003, SR-002 | switch audit terminal outcome threshold-four mixed long-run matrix 검증 | active-standby holdoff `4` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `4`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-046 | FR-003, SR-002 | switch audit terminal outcome threshold-five mixed long-run matrix 검증 | active-standby holdoff `5` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `5`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-047 | FR-003, SR-002 | switch audit terminal outcome threshold-six mixed long-run matrix 검증 | active-standby holdoff `6` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `6`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-048 | FR-003, SR-002 | switch audit terminal outcome threshold-seven mixed long-run matrix 검증 | active-standby holdoff `7` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `7`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-050 | FR-003, SR-002 | switch audit terminal outcome threshold-eight mixed long-run matrix 검증 | active-standby holdoff `8` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `8`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-051 | FR-003, SR-002 | switch audit terminal outcome threshold-nine mixed long-run matrix 검증 | active-standby holdoff `9` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `9`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-052 | FR-003, SR-002 | switch audit terminal outcome threshold-ten mixed long-run matrix 검증 | active-standby holdoff `10` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `10`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-053 | FR-003, SR-002 | switch audit terminal outcome threshold-eleven mixed long-run matrix 검증 | active-standby holdoff `11` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `11`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-054 | FR-003, SR-002 | switch audit terminal outcome threshold-twelve mixed long-run matrix 검증 | active-standby holdoff `12` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `12`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-055 | FR-003, SR-002 | switch audit terminal outcome threshold-thirteen mixed long-run matrix 검증 | active-standby holdoff `13` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `13`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-056 | FR-003, SR-002 | switch audit terminal outcome threshold-fourteen mixed long-run matrix 검증 | active-standby holdoff `14` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `14`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-057 | FR-003, SR-002 | switch audit terminal outcome threshold-fifteen mixed long-run matrix 검증 | active-standby holdoff `15` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `15`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-049 | FR-003, SR-002 | switch audit terminal outcome stability long-run matrix 검증 | ordinary complete 이후 active no-op, abort/reset, bypass complete 이후 active no-op까지 한 run 안에서 재현 가능한 active-standby session-supervisor 준비 | `failover -> hold -> recovery -> active noop -> failover -> hold -> reset -> hold -> bypass complete -> active noop` 순서의 representative flow를 실행 | terminal outcome family는 terminal outcome이 이미 기록된 뒤의 repeated active refresh/no-op가 subtype/total count, last trigger, last completed subtype, last holdoff state를 오염시키지 않고 final established state를 유지한다 | terminal outcome subtype/total counts, trigger-origin/event counts, `eLastTerminalHoldoffOutcome`, `eLastTerminalHoldoffOutcomeTriggerEventType`, `eLastTerminalHoldoffOutcomeTriggerChannelId`, `eLastCompletedHoldoffCycleKind`, `eLastHoldoffCycleState`, final established state가 설계와 일치 |
| TC-SUP-025 | FR-003, SR-002 | timer delegation matrix 검증 | `CONNECTING`, `ESTABLISHED`, `RETRANSMISSION_PENDING` 상태 session과 invalid timer source 구성이 가능한 supervisor 준비 | `SUPERVISION` expiry를 `CONNECTING`/`ESTABLISHED`에서, `RETRANSMISSION` expiry를 `RETRANSMISSION_PENDING`에서, `INVALID` source를 `ESTABLISHED`에서 각각 처리 | supervision expiry는 `TIMEOUT_EXPIRED` fail-safe로 위임되고, retransmission expiry는 `RETRANSMISSION_FAILED` fail-safe로 위임되며, invalid source는 session state를 유지한 채 `SESSION_ERROR`로 반환된다 | timer source별 session status, reason, supervisor decision, accepted/error decision counter, final session state가 설계와 일치 |
| TC-SUP-026 | FR-003, SR-002 | poll receive retry ordering matrix 검증 | `ESTABLISHED` 상태 session, `query/receive` status와 valid inbound frame을 단계별로 제어할 수 있는 supervisor 준비 | `RX_ERROR -> UNAVAILABLE(no frame) -> RX_ERROR -> VALID_DATA -> unavailable channel query` 순서로 `poll_receive`를 호출 | generic receive error는 budget을 증가시키지만, no-frame/valid inbound/channel-gated path는 retry budget을 reset하며, success path는 application callback까지 정상 수행한다 | receive error count/reset telemetry, decision type, success callback, final channel-gated status가 설계와 일치 |
| TC-SUP-027 | FR-003, SR-002 | pump receive terminal ordering matrix 검증 | `ESTABLISHED` 상태 session에서 scripted receive/no-frame/channel-gated 종료 조건을 구분해 준비 | `FRAME_RECEIVED -> UNAVAILABLE`, immediate `UNAVAILABLE`, immediate unavailable channel query 각각에 대해 `pump_receive(max_polls)` 호출 | processed frame이 있는 뒤 `NO_FRAME`이 오면 `OK`로 종료하고, 첫 iteration에서 바로 `NO_FRAME`이면 `NO_FRAME`, 첫 iteration에서 channel-gated면 `CHANNEL_DOWN`으로 종료한다 | terminal status, `uLastPumpIterationCount`, `uLastPumpProcessedFrameCount`, terminal decision이 설계와 일치 |
| TC-SUP-028 | FR-003, SR-002 | pump receive error ordering matrix 검증 | `ESTABLISHED` 상태 session에서 scripted receive/channel-down/decode-failure 종료 조건을 구분해 준비 | `FRAME_RECEIVED -> CHANNEL_DOWN`, `FRAME_RECEIVED -> DECODE_FAILED` 각각에 대해 `pump_receive(max_polls)` 호출 | processed frame이 먼저 있었더라도 뒤따르는 `CHANNEL_DOWN`과 `DECODE_FAILED`는 `OK`로 흡수되지 않고 terminal error로 그대로 반환된다 | terminal status, `uLastPumpIterationCount`, `uLastPumpProcessedFrameCount`, terminal decision이 설계와 일치 |
| TC-SUP-029 | FR-003, SR-002 | pump receive ignored ordering matrix 검증 | `ESTABLISHED` 상태 session에서 immediate/processed-then-budgeted receive error 경로를 구분해 준비 | immediate `RX_ERROR`, `FRAME_RECEIVED -> RX_ERROR` 각각에 대해 `pump_receive(max_polls)` 호출 | `pump_receive`는 `NO_FRAME`과 달리 `IGNORED_EVENT`를 terminal로 즉시 반환하며, 이미 처리한 frame이 있으면 processed count를 보존한 채 budgeted decision을 보고한다 | terminal status, `uLastPumpIterationCount`, `uLastPumpProcessedFrameCount`, receive-error budgeted decision이 설계와 일치 |
| TC-SUP-030 | FR-003, SR-002 | pump receive escalation ordering matrix 검증 | `ESTABLISHED` 상태 session에서 receive error budget이 이미 `1`회 누적된 supervisor를 준비 | budget을 priming한 뒤 추가 `RX_ERROR`와 함께 `pump_receive(max_polls)` 호출 | receive error budget이 소진되어 fail-safe escalation이 발생하면 `pump_receive`는 `OK`를 유지한 채 session을 `SAFE_DISCONNECT`로 만들고 escalated decision/effective event를 보고한다 | terminal status, `uLastPumpIterationCount`, `uLastPumpProcessedFrameCount`, escalated decision/effective event, final session state가 설계와 일치 |
| TC-SUP-031 | FR-003, SR-002 | pump receive max-poll ordering matrix 검증 | `ESTABLISHED` 상태 session에서 scripted valid inbound frame이 `max_polls`보다 많이 준비된 supervisor를 준비 | valid frame 3건이 준비된 상태에서 `pump_receive(2)` 호출 | terminal event가 없으면 `pump_receive`는 `max_polls`까지만 처리하고 `OK`로 종료하며, processed/iteration count는 실제 loop budget에 맞게 기록된다 | terminal status, `uLastPumpIterationCount`, `uLastPumpProcessedFrameCount`, cumulative processed count, final established state가 설계와 일치 |
| TC-SUP-032 | FR-003, SR-002 | runtime ordering closeout matrix 검증 | `ESTABLISHED` 상태 active-standby session-supervisor에서 budget scope, stale feedback, channel event, timer delegation, poll/pump receive ordering을 matrix 단위로 재현할 수 있는 준비 | budget scope, send feedback ordering, channel event ordering, timer delegation, poll receive retry ordering, pump receive terminal/error/ignored/escalation/max-poll matrix를 representative closeout wrapper로 실행 | supervisor runtime loop의 핵심 ordering 규칙은 channel-scoped send budget, non-channel-scoped receive budget, stale feedback filtering, channel switch telemetry, timer delegation, poll/pump terminal semantics 관점에서 서로 충돌 없이 일관되게 유지된다 | 각 matrix의 decision/status/telemetry가 누락 없이 실행되고 representative closeout wrapper가 전체 runtime ordering 규칙군을 포괄함이 설계와 일치 |
| TC-SUP-011 | SR-002 | timer expiry delegation 검증 | `CONNECTING` 상태 session 준비 | supervision timer expiry 처리 | `SAFE_DISCONNECT` 전이 | timer ingress가 supervisor 경계를 통해 session으로 전달되고 session status가 보고된다 |
| TC-SUP-016 | FR-003, SR-002 | decision class telemetry 검증 | representative accepted/error/ignored/rejected path 준비 | handshake, decode failure, failover ignored, channel up refresh, send failure escalation, timer delegation 경로 수행 | 각 경로가 expected decision class와 cumulative counter를 보고 | decision class mapping과 누적 카운터가 설계와 일치 |

| TC-SUP-058 | FR-003, SR-002 | switch audit terminal outcome threshold-sixteen mixed long-run matrix 검증 | active-standby holdoff `16` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `16`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-060 | FR-003, SR-002 | switch audit terminal outcome threshold-seventeen mixed long-run matrix 검증 | active-standby holdoff `17` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `17`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-061 | FR-003, SR-002 | switch audit terminal outcome threshold-eighteen mixed long-run matrix 검증 | active-standby holdoff `18` config에서 ordinary complete, abort, bypass complete를 한 run 안에서 모두 재현 가능한 session-supervisor 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery`, `failover -> hold -> reset`, `failover -> hold -> bypass complete` 순서의 representative flow를 실행 | threshold `18`에서도 terminal outcome family는 ordinary complete, abort, bypass complete cumulative parity를 유지하고 holdoff progress/remaining은 threshold-aware policy에 맞게 누적되며 final established state를 유지한다 | `uPreferredRecoveryHoldoffProgressCount`, `uPreferredRecoveryHoldoffRemainingCount`, terminal outcome subtype/total counts, last start trigger, last completed subtype, last terminal outcome이 설계와 일치 |
| TC-SUP-062 | FR-003, SR-002 | switch audit active-loss bypass long-run matrix 검증 | active-standby holdoff `2` config에서 failover/hold/bypass/restore cycle을 두 번 반복해 actual-switch/no-op/terminal-outcome 누적을 함께 관찰할 수 있는 session-supervisor 준비 | `failover -> hold -> secondary CHANNEL_DOWN bypass -> secondary CHANNEL_UP refresh` cycle을 2회 반복 | repeated active-loss bypass cycle에서도 preferred-triggered failover actual switch, non-preferred-triggered bypass actual switch, holdoff no-op, active refresh no-op, bypass terminal outcome과 completed-cycle taxonomy가 서로 오염되지 않고 누적 parity를 유지한다 | `uPreferredChannelTriggeredSwitchCount`, `uNonPreferredChannelTriggeredSwitchCount`, `uHoldoffPreferredRecoverySwitchCount`, `uBypassPreferredRecoverySwitchCount`, `uCompletedHoldoffCycleCount`, `uBypassCompletedHoldoffCycleCount`, `uTerminalHoldoffOutcomeCount`, `uBypassTerminalHoldoffOutcomeCount`, `uPreferredChannelTriggeredRefreshEventCount`, `uNonPreferredChannelTriggeredRefreshEventCount`, `uHoldoffRefreshNoOpCount`, `uActiveRefreshNoOpCount`, `eLastCompletedHoldoffCycleKind`, `eLastTerminalHoldoffOutcome`, `eLastTerminalHoldoffOutcomeTriggerEventType`, `eLastTerminalHoldoffOutcomeTriggerChannelId`가 설계와 일치 |
