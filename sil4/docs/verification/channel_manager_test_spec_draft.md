# Test Specification Draft - Channel Manager

## Document Control

- Document ID: `TS-015`
- Version: `0.1.0`
- Status: `Draft`
- Owner: `Project Team`
- Last Updated: `2026-04-28`

## Scope

- 테스트 대상:
  - `MOD-010 Channel Manager`
- 관련 요구사항:
  - `FR-003`
  - `IF-002`
  - `SR-003`
- 테스트 레벨: `Unit`

## Test Cases

| Test ID | Req ID | Objective | Precondition | Stimulus | Expected Result | Pass/Fail Criteria |
| --- | --- | --- | --- | --- | --- | --- |
| TC-CHM-001 | FR-003, IF-002 | preferred channel 초기 선택 검증 | primary/secondary available config 준비 | `init`, `select_channel` 호출 | primary 선택, failover 없음 | selected channel, available count, failover flag가 설계와 일치 |
| TC-CHM-049 | FR-003, SR-003, IF-002 | invalid redundancy topology config 거부 검증 | duplicate channel id, active-standby single-channel, valid single-channel config 준비 | 각각 `rsrx_channel_manager_init` 호출 | duplicate channel id와 active-standby single-channel config는 `INVALID_ARGUMENT`로 거부되고, single-channel `SINGLE` mode는 허용된다 | topology validation이 ambiguous active-standby/duplicate channel selection을 startup에서 결정적으로 차단 |
| TC-CHM-050 | FR-003, SR-003, IF-002 | runtime topology mutation 거부 검증 | primary/secondary active-standby config가 init 완료된 상태 | primary index에 secondary channel id를 가진 update를 호출한 뒤 select 수행 | update는 `INVALID_ARGUMENT`로 거부되고 기존 primary topology와 availability/switch count가 유지된다 | runtime update가 configured channel identity를 변질시키지 않고 availability-only update contract를 유지한다 |
| TC-CHM-051 | FR-003, SR-003, IF-002 | duplicate channel priority topology 거부 검증 | active-standby channel id는 서로 다르지만 priority가 같은 config 준비 | `rsrx_channel_manager_init` 호출 | `INVALID_ARGUMENT`로 거부된다 | failover best-channel selection이 duplicate priority/array-order tie에 의존하지 않도록 startup에서 차단된다 |
| TC-CHM-002 | FR-003 | active channel unavailable 시 failover 검증 | primary down, secondary up 상태 준비 | `update_channel`, `select_channel` 호출 | secondary 선택, failover 발생 | active channel, failover flag, cumulative switch count가 설계와 일치 |
| TC-CHM-003 | SR-003 | all channel unavailable telemetry 검증 | primary/secondary 모두 down 상태 준비 | `select_channel` 호출 | `UNAVAILABLE` 반환 | invalid channel, unavailable status, cumulative unavailable selection count가 결정적으로 보고되고 recovery/reset 이후에도 audit count가 유지됨 |
| TC-CHM-005 | FR-003 | preferred channel recovery auto-switch 검증 | secondary로 failover된 뒤 primary restored 상태 준비 | `update_channel`, `select_channel` 호출 | preferred primary로 자동 복귀 | selected channel, switch flag, cumulative switch count가 preferred recovery policy와 일치 |
| TC-CHM-006 | FR-003 | preferred recovery holdoff 검증 | secondary로 failover된 뒤 primary restored, holdoff `2` config 준비 | `select_channel`을 2회 연속 호출 | 첫 호출은 secondary 유지, 두 번째 호출에서 primary 복귀 | holdoff count 전에는 switch가 억제되고 threshold 도달 후에만 복귀하며 cumulative switch count가 기대값과 일치 |
| TC-CHM-052 | FR-003, SR-003, IF-002 | preferred recovery holdoff result telemetry 검증 | secondary로 failover된 뒤 primary restored, holdoff `2` config 준비 | failover, first stable select, recovery select를 순차 실행 | failover 전후 result가 holdoff active flag, progress, target, remaining count를 deterministic하게 보고한다 | holdoff active/progress/target/remaining telemetry가 selection policy와 supervisor audit cross-check에 사용할 수 있는 값으로 유지된다 |
| TC-CHM-053 | FR-003, SR-003, IF-002 | preferred recovery flap-penalty holdoff 검증 | secondary로 failover된 뒤 primary restore, first hold, preferred flap-down, restored-again recovery를 재현 가능한 holdoff `2` + flap-penalty `1` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | preferred flap-down은 current holdoff progress를 reset하고 다음 recovery cycle의 effective holdoff target을 `2 -> 3`으로 늘리며, restored-again 뒤에는 세 번째 stable select에서만 primary 복귀가 허용되고 복귀 후 target은 다시 base `2`로 돌아간다 | selected channel, switch flag, holdoff progress, pending penalty, penalty-arm count, penalty-clear count, target, remaining telemetry가 flap-penalty policy와 일치 |
| TC-CHM-054 | FR-003, SR-003, IF-002 | preferred recovery flap-penalty bypass clear 검증 | secondary failover 뒤 first hold, preferred flap-down, renewed hold, active secondary loss bypass를 재현 가능한 holdoff `2` + flap-penalty `1` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> secondary down bypass` 순서로 `update_channel`, `select_channel` 호출 | armed pending penalty는 active-loss bypass로 preferred primary가 선택되는 순간 clear되고 ordinary clear count는 증가하지 않으며 bypass-clear count만 `0 -> 1`로 증가한다 | selected channel, pending penalty, penalty arm/ordinary-clear/bypass-clear count가 flap-penalty bypass-clear policy와 일치 |
| TC-CHM-055 | FR-003, SR-003, IF-002 | preferred recovery flap-penalty applied cycle count 검증 | secondary failover 뒤 first hold, preferred flap-down, restored-again penalty holdoff, preferred recovery 완료를 재현 가능한 holdoff `2` + flap-penalty `1` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | armed pending penalty는 restored-again holdoff cycle이 실제 시작되는 첫 stable select에서만 applied-cycle count를 `0 -> 1`로 증가시키고 이후 hold/recovery 동안 count를 유지한다 | pending penalty, penalty arm/applied/clear count, selected channel이 flap-penalty applied-cycle policy와 일치 |
| TC-CHM-007 | FR-003, SR-003, IF-002 | preferred recovery hysteresis reset matrix 검증 | secondary로 failover된 뒤 primary restore/flap/restore가 반복되는 holdoff `2` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> recovery -> no-op refresh` 순서로 `update_channel`, `select_channel` 호출 | flap down은 holdoff 누적을 reset하고 renewed holdoff 뒤에만 primary 복귀가 허용되며, 복귀 뒤 repeated select는 no-op refresh로 남는다 | selected channel, switch flag, cumulative switch count가 hysteresis reset/no-op refresh policy와 일치 |
| TC-CHM-008 | FR-003, SR-003, IF-002 | active-channel loss가 preferred recovery holdoff를 bypass하는지 검증 | secondary로 failover된 뒤 primary restored, first hold가 누적된 holdoff `2` config 준비 | `failover -> primary restore -> hold -> secondary flap down -> select -> secondary restore -> select` 순서로 `update_channel`, `select_channel` 호출 | holdoff가 누적 중이어도 active secondary가 unavailable이 되면 preferred primary가 즉시 선택되고, 이후 secondary 복구는 no-op refresh로 남는다 | selected channel, switch flag, cumulative switch count가 active-loss bypass/no-op refresh policy와 일치 |
| TC-CHM-009 | FR-003, SR-003, IF-002 | active-loss bypass 이후 다음 cycle에서도 holdoff가 다시 적용되는지 검증 | secondary로 failover된 뒤 primary restore/hold, secondary loss bypass, secondary restore refresh를 한 번 거친 holdoff `2` config 준비 | `first failover -> hold -> secondary loss bypass -> refresh -> second failover -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | active-loss bypass는 즉시 preferred primary 복귀를 허용하지만, 이후 다음 failover cycle에서는 holdoff가 새로 누적되어야만 다시 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 bypass 이후 holdoff re-entry policy와 일치 |
| TC-CHM-010 | FR-003, SR-003, IF-002 | flap reset과 active-loss bypass를 거친 뒤 다음 cycle에서도 holdoff가 다시 적용되는지 검증 | secondary로 failover된 뒤 primary restore/hold, primary flap reset, renewed hold, secondary loss bypass, secondary restore refresh를 한 번 거친 holdoff `2` config 준비 | `first failover -> hold -> primary flap down -> renewed hold -> secondary loss bypass -> refresh -> second failover -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff 누적을 초기화하고, active-loss bypass는 즉시 preferred primary 복귀를 허용하지만, 이후 다음 failover cycle에서는 holdoff가 다시 새로 누적되어 첫 recovery signal에서는 secondary를 유지하고 두 번째 recovery signal에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 flap reset + bypass + holdoff re-entry policy와 일치 |
| TC-CHM-011 | FR-003, SR-003, IF-002 | preferred recovery hysteresis closeout matrix 검증 | holdoff reset, active-loss bypass, bypass 이후 holdoff re-entry, flap-reset 이후 bypass re-entry를 모두 재현 가능한 holdoff `2` config 준비 | hysteresis reset matrix, active-loss bypass matrix, bypass re-entry matrix, flap-reset+bypass re-entry matrix를 closeout wrapper로 실행 | preferred recovery hysteresis 규칙군에서 holdoff reset, bypass, bypass 이후 re-entry semantics가 서로 모순 없이 유지된다 | representative wrapper가 channel manager hysteresis 규칙군의 unit coverage를 하나의 closeout 항목으로 추적 가능하게 유지한다 |
| TC-CHM-012 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `3` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `3` config 준비 | `failover -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 두 번의 stable select는 secondary를 유지하고 세 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 higher holdoff threshold policy와 일치 |
| TC-CHM-013 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `4` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `4` config 준비 | `failover -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 세 번의 stable select는 secondary를 유지하고 네 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 higher holdoff threshold policy와 일치 |
| TC-CHM-018 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `5` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `5` config 준비 | `failover -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 네 번의 stable select는 secondary를 유지하고 다섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-020 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `6` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `6` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 다섯 번의 stable select는 secondary를 유지하고 여섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-022 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `7` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `7` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 여섯 번의 stable select는 secondary를 유지하고 일곱 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-024 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `8` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `8` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 일곱 번의 stable select는 secondary를 유지하고 여덟 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-026 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `9` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `9` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 여덟 번의 stable select는 secondary를 유지하고 아홉 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-028 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `10` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `10` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 아홉 번의 stable select는 secondary를 유지하고 열 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-030 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `11` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `11` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열 번의 stable select는 secondary를 유지하고 열한 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-032 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `12` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `12` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열한 번의 stable select는 secondary를 유지하고 열두 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-034 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `13` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `13` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열두 번의 stable select는 secondary를 유지하고 열세 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-036 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `14` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `14` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열세 번의 stable select는 secondary를 유지하고 열네 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-038 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `15` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `15` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열네 번의 stable select는 secondary를 유지하고 열다섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-040 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `16` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `16` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열다섯 번의 stable select는 secondary를 유지하고 열여섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-042 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `17` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `17` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열여섯 번의 stable select는 secondary를 유지하고 열일곱 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-044 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `18` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `18` config 준비 | `failover -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> hold -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열일곱 번의 stable select는 secondary를 유지하고 열여덟 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-046 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `19` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `19` config 준비 | `failover -> hold x18 -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열여덟 번의 stable select는 secondary를 유지하고 열아홉 번째 stable select에서만 primary 복귀가 허용된다 | loop 기반 selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-048 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `20` parity 검증 | secondary로 failover된 뒤 primary restored 상태의 holdoff `20` config 준비 | `failover -> hold x19 -> recovery` 순서로 `update_channel`, `select_channel` 호출 | 첫 열아홉 번의 stable select는 secondary를 유지하고 스무 번째 stable select에서만 primary 복귀가 허용된다 | loop 기반 selected channel, switch flag, cumulative switch count가 configurable higher holdoff threshold policy와 일치 |
| TC-CHM-014 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `3` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `3` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `3` 누적을 초기화하고 restored again 뒤에는 다시 세 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 higher-threshold flap-reset policy와 일치 |
| TC-CHM-015 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `4` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `4` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `4` 누적을 초기화하고 restored again 뒤에는 다시 네 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 higher-threshold flap-reset policy와 일치 |
| TC-CHM-019 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `5` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `5` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `5` 누적을 초기화하고 restored again 뒤에는 다시 다섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-021 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `6` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `6` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `6` 누적을 초기화하고 restored again 뒤에는 다시 여섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-023 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `7` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `7` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `7` 누적을 초기화하고 restored again 뒤에는 다시 일곱 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-025 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `8` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `8` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `8` 누적을 초기화하고 restored again 뒤에는 다시 여덟 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-027 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `9` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `9` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `9` 누적을 초기화하고 restored again 뒤에는 다시 아홉 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-029 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `10` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `10` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `10` 누적을 초기화하고 restored again 뒤에는 다시 열 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-031 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `11` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `11` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `11` 누적을 초기화하고 restored again 뒤에는 다시 열한 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-033 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `12` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `12` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `12` 누적을 초기화하고 restored again 뒤에는 다시 열두 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-035 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `13` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `13` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `13` 누적을 초기화하고 restored again 뒤에는 다시 열세 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-037 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `14` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `14` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `14` 누적을 초기화하고 restored again 뒤에는 다시 열네 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-039 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `15` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `15` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `15` 누적을 초기화하고 restored again 뒤에는 다시 열다섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-041 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `16` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `16` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `16` 누적을 초기화하고 restored again 뒤에는 다시 열여섯 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-043 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `17` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `17` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `17` 누적을 초기화하고 restored again 뒤에는 다시 열일곱 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-045 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `18` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `18` config 준비 | `failover -> hold -> primary flap down -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed hold -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `18` 누적을 초기화하고 restored again 뒤에는 다시 열여덟 번째 stable select에서만 primary 복귀가 허용된다 | selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-047 | FR-003, SR-003, IF-002 | preferred recovery holdoff threshold `19` flap-reset parity 검증 | secondary로 failover된 뒤 primary restored, first hold 누적, primary flap reset, restored again 상태의 holdoff `19` config 준비 | `failover -> hold -> primary flap down -> renewed hold x18 -> renewed recovery` 순서로 `update_channel`, `select_channel` 호출 | flap reset은 holdoff `19` 누적을 초기화하고 restored again 뒤에는 다시 열아홉 번째 stable select에서만 primary 복귀가 허용된다 | loop 기반 selected channel, switch flag, cumulative switch count가 configurable higher-threshold flap-reset policy와 일치 |
| TC-CHM-016 | FR-003, SR-003, IF-002 | preferred recovery threshold closeout matrix 검증 | holdoff threshold `3..20` pure parity와 flap-reset parity `3..19`를 모두 재현 가능한 config 준비 | threshold `3..20`와 threshold `3..19 + flap-reset` matrix를 closeout wrapper로 실행 | higher-threshold hysteresis 규칙군에서 pure threshold growth `3..20`과 flap-reset interaction `3..19`가 서로 모순 없이 유지된다 | representative wrapper가 higher-threshold hysteresis 규칙군의 unit coverage를 하나의 closeout 항목으로 추적 가능하게 유지한다 |
| TC-CHM-017 | FR-003, SR-003, IF-002 | preferred recovery stability envelope closeout matrix 검증 | hysteresis reset/bypass/re-entry와 higher-threshold parity를 모두 재현 가능한 active-standby config 준비 | hysteresis closeout matrix와 threshold closeout matrix를 representative wrapper로 실행 | current preferred recovery hysteresis 규칙군에서 holdoff reset, bypass, threshold growth, flap-reset interaction이 서로 모순 없이 유지된다 | representative wrapper가 channel manager stability envelope coverage를 하나의 closeout 항목으로 추적 가능하게 유지한다 |
| TC-CHM-004 | FR-003 | reset to preferred channel 검증 | failover 후 primary restored 상태 준비 | `reset`, `select_channel` 호출 | preferred primary 재선택 | selected channel이 preferred policy와 일치 |
